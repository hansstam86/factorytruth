# Secret for signing session cookies. Use a long random string in production.
JWT_SECRET=your-secret-at-least-32-chars-long

# Factory registration: only company emails are allowed (no Gmail, QQ, etc.).

# Entrepreneur SSO (optional). Get credentials from Google Cloud Console:
# APIs & Services → Credentials → Create OAuth 2.0 Client ID (Web application).
# Add redirect URI: https://yourdomain.com/api/entrepreneur-auth/sso/callback
# GOOGLE_CLIENT_ID=...
# GOOGLE_CLIENT_SECRET=...

# App URL for OAuth redirects (production). e.g. https://www.factorytruth.com
# NEXT_PUBLIC_APP_URL=https://www.factorytruth.com

# Translation: factory answers (Chinese) → English for entrepreneurs. Optional.
# Get a key at https://www.deepl.com/pro-api (free tier available).
# Without this, entrepreneurs see the original Chinese answers.
# DEEPL_AUTH_KEY=your-deepl-auth-key
# DEEPL_API_URL=https://api-free.deepl.com/v2/translate

# Admin: edit audit questions at /admin (English). Set both to enable admin login.

# Bulk import: email used as userId for factories that have no email in the CSV.
# BULK_IMPORT_PLACEHOLDER_EMAIL=hans.stam@gmail.com

